spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        ddl-auto: validate

  application:
    name: demo

  datasource:
    url: jdbc:mariadb://localhost:3306/web_store
    username: webstore
    password: webstore_password

server:
  port: 8090

